/** App-wide constants shared between server + client code. */

/** Default password assigned to every seeded and admin-created user.
 *  Stored bcrypt-hashed in `public.user_credentials`. */
export const DEFAULT_PASSWORD = 'NAUB@2026';
